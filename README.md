## 🧠 How the search works

1. User enters a keyword
2. Input is normalized (lowercase)
3. Data is filtered using string matching
4. Matching results are dynamically rendered

This simulates how basic search systems work in real applications.
